fastapi>=0.85.0
uvicorn>=0.18.0
sqlalchemy>=1.4
passlib[bcrypt]>=1.7
# passlib[bcrypt] is used for password hashing and verification helpers.
# Optional: email validation support used by Pydantic EmailStr
email-validator>=2.0.0
# bcrypt wheels keep local installs simple on common platforms.
# Optional for local env loading in future tooling
python-dotenv>=1.0.0
# Keep dotenv optional so production can rely on real environment variables.

# Keep this file minimal and aligned with the app's actual imports.
# Move optional/dev dependencies to separate files if this grows.
# Pinning stays loose here to keep local setup friction low.
# The core set is enough to start the app and exercise endpoints.
