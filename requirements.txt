fastapi>=0.85.0
uvicorn>=0.18.0
sqlalchemy>=1.4
passlib[bcrypt]>=1.7
# Optional: email validation support used by Pydantic EmailStr
email-validator>=2.0.0
# Optional for local env loading in future tooling
python-dotenv>=1.0.0

# Keep this file minimal and aligned with the app's actual imports.
# Move optional/dev dependencies to separate files if this grows.
# Pinning stays loose here to keep local setup friction low.
