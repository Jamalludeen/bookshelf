# Copy to .env and adjust as needed.
DATABASE_URL=sqlite:///./sql_app.db
# Default points to local sqlite file for zero-config startup.

# Example only; do not commit real credentials here.
# Keep this file committed so onboarding stays easy.
# Adjust the URL if you want to use a different local database path.
# Keep the format consistent with SQLAlchemy URL syntax.
# Adjust only if you know the target database path is correct.
